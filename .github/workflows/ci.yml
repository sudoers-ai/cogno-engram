name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Lint (ruff)
        run: ruff check cogno_engram cognobench tests
      - name: Type-check (mypy)
        run: mypy cogno_engram
      - name: Unit + bench-smoke tests
        # Postgres integration tests auto-skip without ENGRAM_TEST_DSN.
        run: pytest -q
      - name: EngramBench gate
        run: python3 cognobench.py --min-score 100

  integration:
    # The Postgres + pgvector adapter, exercised against a real service container.
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,postgres]"
      - name: Postgres integration tests
        env:
          ENGRAM_TEST_DSN: postgresql://postgres:postgres@localhost:5432/postgres
        run: pytest tests/test_postgres_integration.py -q
